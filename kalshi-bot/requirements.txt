# Runtime dependencies. Kept deliberately small.
#
# The safety-critical core (fees, risk gates, storage, no-lookahead
# enforcement) depends ONLY on the Python standard library, so those tests
# run with nothing installed. The packages below are needed only for live
# I/O (signing requests, parsing config, the websocket feed).
cryptography>=42.0      # RSA-PSS request signing for Kalshi auth
PyYAML>=6.0             # config files
websockets>=12.0        # Kalshi real-time order-book feed
